# Clhoria Template 开发运行流程

## 一、环境要求

### 必需环境

- **Node.js**: >= 24.12.0
- **pnpm**: >= 10.24.0
- **PostgreSQL**: >= 18 (推荐) 或 >= 13 (需修改配置)
- **Redis**: >= 7

### 可选工具

- **Docker**: 用于容器化部署
- **Git**: 版本控制

---

## 二、数据库安装配置

### 方式 1: Docker 快速启动 (推荐)

使用项目自带的 `docker-compose.yml` 一键启动 PostgreSQL + Redis：

```bash
# 启动数据库服务
docker-compose up -d postgres redis

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f postgres redis
```

**默认配置**:

- PostgreSQL: `localhost:5432`
  - 用户名: `postgres`
  - 密码: `postgres`
  - 数据库: `postgres`
- Redis: `localhost:6379`

### 方式 2: 本地安装

#### macOS (使用 Homebrew)

```bash
# 安装 PostgreSQL
brew install postgresql@18
brew services start postgresql@18

# 安装 Redis
brew install redis
brew services start redis

# 创建数据库
createdb postgres
```

#### Linux (Ubuntu/Debian)

```bash
# 安装 PostgreSQL
sudo apt update
sudo apt install postgresql postgresql-contrib

# 启动服务
sudo systemctl start postgresql
sudo systemctl enable postgresql

# 安装 Redis
sudo apt install redis-server
sudo systemctl start redis-server

# 创建数据库用户和数据库
sudo -u postgres psql
CREATE DATABASE postgres;
CREATE USER postgres WITH PASSWORD 'postgres';
GRANT ALL PRIVILEGES ON DATABASE postgres TO postgres;
\q
```

#### Windows

推荐使用 Docker Desktop 或下载安装包：

- PostgreSQL: https://www.postgresql.org/download/windows/
- Redis: https://github.com/microsoftarchive/redis/releases

---

## 三、项目初始化

### 1. 克隆项目

```bash
git clone https://github.com/zhe-qi/clhoria-template
cd clhoria-template
```

### 2. 安装 pnpm (如未安装)

```bash
# 启用 Node.js 自带的 corepack
npm i -g corepack
corepack enable

# 验证安装
pnpm --version
```

### 3. 安装依赖

```bash
pnpm install
```

---

## 四、环境变量配置

### 1. 创建环境变量文件

```bash
# 复制示例文件
cp .env.example .env
```

### 2. 配置必需环境变量

编辑 `.env` 文件，配置以下关键参数：

```bash
# 运行环境
NODE_ENV=development

# 服务端口
PORT=9999

# 日志级别
LOG_LEVEL=debug

# 数据库连接 (根据实际情况修改)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Redis 连接
REDIS_URL=redis://localhost:6379/0

# JWT 密钥 (必须修改为强随机字符串，至少 32 字符)
CLIENT_JWT_SECRET=your-client-jwt-secret-min-32-chars-long-random-string
ADMIN_JWT_SECRET=your-admin-jwt-secret-min-32-chars-long-random-string

# 对象存储配置 (可选，用于文件上传)
ACCESS_KEY_ID=your-access-key-id
SECRET_ACCESS_KEY=your-secret-access-key
ENDPOINT=https://your-r2-endpoint.com
BUCKET_NAME=your-bucket-name

# Sentry 错误追踪 (可选)
SENTRY_DSN=your-sentry-dsn
```

**生成强随机密钥**:

```bash
# 使用 Node.js 生成
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# 或使用 openssl
openssl rand -hex 32
```

---

## 五、数据库初始化

### 开发环境 (推荐)

使用 `pnpm push` 直接推送 schema 到数据库，无需生成迁移文件：

```bash
# 推送数据库 schema
pnpm push

# 填充初始数据 (可选，应用启动时会自动检查并初始化)
pnpm seed
```

**初始账号**:

- 管理员: `admin` / `123456`
- 普通用户: `user` / `123456`

### 生产环境

生产环境需要生成迁移文件，便于版本控制和回滚：

```bash
# 1. 生成迁移文件
pnpm generate

# 2. 执行迁移
pnpm migrate

# 3. 填充初始数据
pnpm seed
```

### 其他数据库命令

```bash
# 打开 Drizzle Studio (可视化数据库管理)
pnpm studio

# 查看生成的迁移文件
ls migrations/
```

---

## 六、启动项目

### 开发模式 (热重载)

```bash
pnpm dev
```

启动成功后访问：

- **API 文档**: http://localhost:9999
- **健康检查**: http://localhost:9999/health

### 生产模式

```bash
# 1. 构建项目
pnpm build

# 2. 启动生产服务
pnpm start
```

---

## 七、每日开发流程 (macOS)

### 标准开发启动流程

每次开始开发时，按以下步骤操作：

#### 方式 1: 使用 Docker (推荐)

```bash
# 1. 进入项目目录
cd /Users/nic/Nic\ Projects/clhoria-template

# 2. 启动数据库服务 (PostgreSQL + Redis)
docker-compose up -d postgres redis

# 3. 等待服务就绪 (约 5-10 秒)
docker-compose ps

# 4. 启动开发服务器
pnpm dev
```

**一键启动脚本** (推荐)

创建 `dev-start.sh` 文件：

```bash
#!/bin/bash

echo "🚀 启动开发环境..."
echo ""

# 进入项目目录
cd "$(dirname "$0")"

# 检查 Docker 是否运行
if ! docker info > /dev/null 2>&1; then
    echo "❌ Docker 未运行，请先启动 Docker Desktop"
    exit 1
fi

# 启动数据库服务
echo "📦 启动数据库服务..."
docker-compose up -d postgres redis

# 等待服务就绪
echo "⏳ 等待服务启动..."
sleep 5

# 检查服务状态
echo ""
echo "🔍 检查服务状态..."
docker-compose ps postgres redis

# 检查端口占用
echo ""
echo "🔌 检查端口占用..."
if lsof -i :9999 > /dev/null 2>&1; then
    echo "⚠️  端口 9999 已被占用，请先关闭占用进程"
    lsof -i :9999
    exit 1
fi

# 启动开发服务器
echo ""
echo "✨ 启动开发服务器..."
echo "📖 API 文档: http://localhost:9999"
echo "💚 健康检查: http://localhost:9999/health"
echo ""
pnpm dev
```

赋予执行权限并使用：

```bash
chmod +x dev-start.sh
./dev-start.sh
```

#### 方式 2: 使用本地服务

```bash
# 1. 进入项目目录
cd /Users/nic/Nic\ Projects/clhoria-template

# 2. 启动 PostgreSQL (如果未自动启动)
brew services start postgresql@18

# 3. 启动 Redis (如果未自动启动)
brew services start redis

# 4. 检查服务状态
brew services list | grep -E 'postgresql|redis'

# 5. 启动开发服务器
pnpm dev
```

### 开发中常用命令

```bash
# 查看数据库 (在新终端窗口)
pnpm studio

# 运行测试 (在新终端窗口)
pnpm test

# 代码检查
pnpm lint

# 类型检查
pnpm typecheck
```

### 结束开发流程

#### Docker 方式

```bash
# 1. 停止开发服务器 (Ctrl + C)

# 2. 停止数据库服务 (保留数据)
docker-compose stop

# 或完全关闭并清理
docker-compose down
```

**一键停止脚本**

创建 `dev-stop.sh` 文件：

```bash
#!/bin/bash

echo "🛑 停止开发环境..."
echo ""

# 进入项目目录
cd "$(dirname "$0")"

# 停止所有服务
echo "📦 停止数据库服务..."
docker-compose stop

echo ""
echo "✅ 开发环境已停止"
echo "💡 提示: 数据已保留，下次启动时数据仍然存在"
echo "💡 如需清理数据，运行: docker-compose down -v"
```

赋予执行权限：

```bash
chmod +x dev-stop.sh
./dev-stop.sh
```

#### 本地服务方式

```bash
# 1. 停止开发服务器 (Ctrl + C)

# 2. 停止数据库服务 (可选，通常保持运行)
brew services stop postgresql@18
brew services stop redis
```

### 快速故障排查

开发启动失败时，按以下顺序检查：

```bash
# 1. 检查 Docker 服务状态
docker-compose ps

# 2. 查看服务日志
docker-compose logs postgres redis

# 3. 检查端口占用
lsof -i :9999  # 应用端口
lsof -i :5432  # PostgreSQL
lsof -i :6379  # Redis

# 4. 检查环境变量
cat .env | grep -E 'DATABASE_URL|REDIS_URL|JWT_SECRET'

# 5. 测试数据库连接
psql postgresql://postgres:postgres@localhost:5432/postgres -c "SELECT 1;"

# 6. 测试 Redis 连接
redis-cli ping
```

### 完整的开发启动检查清单

使用以下脚本进行完整检查，创建 `dev-check.sh` 文件：

```bash
#!/bin/bash

echo "🔍 开发环境检查清单"
echo "===================="
echo ""

# 检查项目目录
if [ ! -f "package.json" ]; then
    echo "❌ 不在项目根目录"
    exit 1
fi
echo "✅ 项目目录正确"

# 检查 Docker
if ! docker info > /dev/null 2>&1; then
    echo "❌ Docker 未运行"
    exit 1
fi
echo "✅ Docker 运行中"

# 检查 node_modules
if [ ! -d "node_modules" ]; then
    echo "⚠️  依赖未安装，正在安装..."
    pnpm install
fi
echo "✅ 依赖已安装"

# 检查环境变量
if [ ! -f ".env" ]; then
    echo "❌ .env 文件不存在"
    echo "💡 请运行: cp .env.example .env"
    exit 1
fi
echo "✅ 环境变量文件存在"

# 检查数据库服务
if ! docker-compose ps postgres | grep -q "Up"; then
    echo "⚠️  PostgreSQL 未运行，正在启动..."
    docker-compose up -d postgres
    sleep 3
fi
echo "✅ PostgreSQL 运行中"

# 检查 Redis 服务
if ! docker-compose ps redis | grep -q "Up"; then
    echo "⚠️  Redis 未运行，正在启动..."
    docker-compose up -d redis
    sleep 2
fi
echo "✅ Redis 运行中"

# 检查端口占用
if lsof -i :9999 > /dev/null 2>&1; then
    echo "❌ 端口 9999 已被占用"
    lsof -i :9999
    exit 1
fi
echo "✅ 端口 9999 可用"

# 测试数据库连接
if docker exec hono-postgres pg_isready -U postgres > /dev/null 2>&1; then
    echo "✅ 数据库连接正常"
else
    echo "❌ 数据库连接失败"
    exit 1
fi

# 测试 Redis 连接
if docker exec hono-redis redis-cli ping > /dev/null 2>&1; then
    echo "✅ Redis 连接正常"
else
    echo "❌ Redis 连接失败"
    exit 1
fi

echo ""
echo "🎉 所有检查通过，可以开始开发！"
echo ""
echo "运行以下命令启动开发服务器:"
echo "  pnpm dev"
```

赋予执行权限并使用：

```bash
chmod +x dev-check.sh
./dev-check.sh
```

### 推荐的终端布局

使用 iTerm2 或 Terminal 的多窗口/分屏功能：

```
┌─────────────────────────────────────────┐
│ 窗口 1: pnpm dev (开发服务器)            │
│ 日志输出、热重载信息                     │
├─────────────────────────────────────────┤
│ 窗口 2: pnpm studio (数据库可视化)       │
│ 或 git 命令、测试命令                    │
├─────────────────────────────────────────┤
│ 窗口 3: 编辑器 (VS Code / Cursor)       │
│ 代码编辑                                 │
└─────────────────────────────────────────┘
```

### 常见开发场景

#### 场景 1: 拉取最新代码后

```bash
# 1. 拉取代码
git pull

# 2. 更新依赖
pnpm install

# 3. 同步数据库 schema
pnpm push

# 4. 启动开发服务器
pnpm dev
```

#### 场景 2: 切换分支后

```bash
# 1. 切换分支
git checkout feature/new-feature

# 2. 更新依赖 (如果 package.json 有变化)
pnpm install

# 3. 同步数据库 (如果 schema 有变化)
pnpm push

# 4. 启动开发服务器
pnpm dev
```

#### 场景 3: 数据库数据错乱需要重置

```bash
# 1. 停止开发服务器 (Ctrl + C)

# 2. 删除数据库容器和数据
docker-compose down -v

# 3. 重新启动数据库
docker-compose up -d postgres redis

# 4. 等待服务就绪
sleep 5

# 5. 推送 schema
pnpm push

# 6. 填充初始数据
pnpm seed

# 7. 启动开发服务器
pnpm dev
```

### 性能优化建议

1. **保持数据库服务运行**: 开发期间不需要频繁停止 PostgreSQL 和 Redis
2. **使用 SSD**: 确保项目在 SSD 上，提升热重载速度
3. **关闭不必要的进程**: 释放系统资源
4. **使用 pnpm**: 比 npm/yarn 更快的依赖管理

---

## 八、代码质量检查

```bash
# 运行 ESLint
pnpm lint

# 自动修复代码风格问题
pnpm lint:fix

# TypeScript 类型检查
pnpm typecheck

# 运行测试
pnpm test

# 完整检查 (lint + typecheck + test)
pnpm check
```

### Git 提交规范

项目配置了 `simple-git-hooks` 和 `lint-staged`，提交前会自动执行：

- ESLint 代码检查
- TypeScript 类型检查

```bash
# 提交代码
git add .
git commit -m "feat: 新增功能"
```

遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具链相关

---

## 九、Docker 部署

### 完整部署 (应用 + 数据库)

```bash
# 启动所有服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f app

# 停止服务
docker-compose down

# 停止并删除数据卷
docker-compose down -v
```

### 仅部署应用

```bash
# 构建镜像
docker build -t clhoria-template .

# 运行容器
docker run -p 9999:9999 --env-file .env clhoria-template
```

---

## 十、环境检查工具

### 1. 检查数据库状态

#### PostgreSQL 状态检查

```bash
# 检查 PostgreSQL 服务状态 (Docker)
docker-compose ps postgres

# 检查 PostgreSQL 服务状态 (macOS)
brew services list | grep postgresql

# 检查 PostgreSQL 服务状态 (Linux)
sudo systemctl status postgresql

# 测试数据库连接
psql postgresql://postgres:postgres@localhost:5432/postgres -c "SELECT version();"

# 查看数据库列表
psql postgresql://postgres:postgres@localhost:5432/postgres -c "\l"

# 查看当前数据库的表
psql postgresql://postgres:postgres@localhost:5432/postgres -c "\dt"
```

#### Redis 状态检查

```bash
# 检查 Redis 服务状态 (Docker)
docker-compose ps redis

# 检查 Redis 服务状态 (macOS)
brew services list | grep redis

# 检查 Redis 服务状态 (Linux)
sudo systemctl status redis

# 测试 Redis 连接
redis-cli ping
# 应返回: PONG

# 查看 Redis 信息
redis-cli info server

# 查看 Redis 内存使用
redis-cli info memory
```

### 2. 检查端口占用情况

#### macOS / Linux

```bash
# 检查应用端口 (9999)
lsof -i :9999

# 检查 PostgreSQL 端口 (5432)
lsof -i :5432

# 检查 Redis 端口 (6379)
lsof -i :6379

# 查看所有监听端口
lsof -iTCP -sTCP:LISTEN -n -P

# 杀死占用端口的进程 (替换 PID 为实际进程 ID)
kill -9 <PID>
```

#### Windows

```bash
# 检查端口占用
netstat -ano | findstr :9999
netstat -ano | findstr :5432
netstat -ano | findstr :6379

# 杀死占用端口的进程 (替换 PID 为实际进程 ID)
taskkill /PID <PID> /F
```

#### 使用 Node.js 脚本检查

创建 `check-ports.js` 文件：

```javascript
const net = require("node:net");

function checkPort(port, host = "localhost") {
  return new Promise((resolve) => {
    const server = net.createServer();

    server.once("error", (err) => {
      if (err.code === "EADDRINUSE") {
        resolve({ port, available: false, message: `端口 ${port} 已被占用` });
      }
      else {
        resolve({ port, available: false, message: err.message });
      }
    });

    server.once("listening", () => {
      server.close();
      resolve({ port, available: true, message: `端口 ${port} 可用` });
    });

    server.listen(port, host);
  });
}

async function main() {
  const ports = [
    { port: 9999, name: "应用服务" },
    { port: 5432, name: "PostgreSQL" },
    { port: 6379, name: "Redis" }
  ];

  console.log("🔍 检查端口占用情况...\n");

  for (const { port, name } of ports) {
    const result = await checkPort(port);
    const icon = result.available ? "✅" : "❌";
    console.log(`${icon} ${name} (${port}): ${result.message}`);
  }
}

main();
```

运行检查：

```bash
node check-ports.js
```

### 3. 一键健康检查脚本

创建 `health-check.sh` 文件：

```bash
#!/bin/bash

echo "🏥 开始环境健康检查..."
echo ""

# 检查 Node.js
echo "📦 Node.js 版本:"
node --version || echo "❌ Node.js 未安装"
echo ""

# 检查 pnpm
echo "📦 pnpm 版本:"
pnpm --version || echo "❌ pnpm 未安装"
echo ""

# 检查 PostgreSQL
echo "🐘 PostgreSQL 状态:"
if command -v psql &> /dev/null; then
    psql --version
    if psql postgresql://postgres:postgres@localhost:5432/postgres -c "SELECT 1;" &> /dev/null; then
        echo "✅ PostgreSQL 连接成功"
    else
        echo "❌ PostgreSQL 连接失败"
    fi
else
    echo "⚠️  psql 命令未找到，检查 Docker 容器..."
    docker-compose ps postgres 2>/dev/null || echo "❌ PostgreSQL 未运行"
fi
echo ""

# 检查 Redis
echo "🔴 Redis 状态:"
if command -v redis-cli &> /dev/null; then
    redis-cli --version
    if redis-cli ping &> /dev/null; then
        echo "✅ Redis 连接成功"
    else
        echo "❌ Redis 连接失败"
    fi
else
    echo "⚠️  redis-cli 命令未找到，检查 Docker 容器..."
    docker-compose ps redis 2>/dev/null || echo "❌ Redis 未运行"
fi
echo ""

# 检查端口占用
echo "🔌 端口占用检查:"
for port in 9999 5432 6379; do
    if lsof -i :$port &> /dev/null; then
        echo "❌ 端口 $port 已被占用"
    else
        echo "✅ 端口 $port 可用"
    fi
done
echo ""

echo "✨ 健康检查完成！"
```

赋予执行权限并运行：

```bash
chmod +x health-check.sh
./health-check.sh
```

---

## 十一、数据库可视化工具

### 1. Drizzle Studio (推荐 - 项目内置)

**优点**: 项目自带，无需额外安装，轻量级

```bash
# 启动 Drizzle Studio
pnpm studio
```

访问 `https://local.drizzle.studio` 即可查看和管理数据库。

**功能**:

- 查看表结构和数据
- 执行 SQL 查询
- 编辑数据
- 查看关系

### 2. DBeaver (免费开源 - 强烈推荐)

**优点**: 功能强大，支持多种数据库，完全免费

- **官网**: https://dbeaver.io/download/
- **支持系统**: Windows、macOS、Linux
- **功能**:
  - 可视化 ER 图
  - SQL 编辑器 (支持自动补全)
  - 数据导入/导出
  - 数据库对比
  - 支持 PostgreSQL、MySQL、SQLite 等

**连接配置**:

- Host: `localhost`
- Port: `5432`
- Database: `postgres`
- Username: `postgres`
- Password: `postgres`

### 3. pgAdmin (PostgreSQL 官方工具)

**优点**: PostgreSQL 官方工具，功能全面

- **官网**: https://www.pgadmin.org/download/
- **支持系统**: Windows、macOS、Linux
- **功能**:
  - 完整的数据库管理
  - 查询工具
  - 备份/恢复
  - 性能监控

### 4. TablePlus (免费版可用)

**优点**: 界面美观，操作流畅

- **官网**: https://tableplus.com/
- **支持系统**: Windows、macOS、Linux
- **免费版限制**: 最多 2 个标签页，2 个窗口
- **功能**:
  - 现代化 UI
  - 多数据库支持
  - SQL 编辑器
  - 数据编辑

### 5. Beekeeper Studio (开源免费)

**优点**: 开源、跨平台、界面简洁

- **官网**: https://www.beekeeperstudio.io/
- **支持系统**: Windows、macOS、Linux
- **功能**:
  - SQL 自动补全
  - 数据编辑
  - 查询历史
  - 多标签页

### 6. DataGrip (JetBrains - 付费)

**优点**: 功能最强大，适合专业开发

- **官网**: https://www.jetbrains.com/datagrip/
- **价格**: 付费 (学生/开源项目可免费)
- **功能**:
  - 智能代码补全
  - 重构工具
  - 版本控制集成
  - 数据库对比

### 7. Redis 可视化工具

#### RedisInsight (Redis 官方 - 免费)

- **官网**: https://redis.com/redis-enterprise/redis-insight/
- **支持系统**: Windows、macOS、Linux
- **功能**:
  - 可视化数据浏览
  - 命令行界面
  - 性能分析
  - 内存分析

#### Another Redis Desktop Manager (开源免费)

- **GitHub**: https://github.com/qishibo/AnotherRedisDesktopManager
- **支持系统**: Windows、macOS、Linux
- **功能**:
  - 多连接管理
  - 数据可视化
  - 命令行工具
  - 支持集群

### 工具对比表

| 工具                 | 类型     | 价格   | 推荐度     | 适用场景            |
| -------------------- | -------- | ------ | ---------- | ------------------- |
| **Drizzle Studio**   | Web      | 免费   | ⭐⭐⭐⭐⭐ | 项目开发、快速查看  |
| **DBeaver**          | 桌面     | 免费   | ⭐⭐⭐⭐⭐ | 全功能数据库管理    |
| **pgAdmin**          | 桌面/Web | 免费   | ⭐⭐⭐⭐   | PostgreSQL 专业管理 |
| **TablePlus**        | 桌面     | 免费版 | ⭐⭐⭐⭐   | 日常开发、界面美观  |
| **Beekeeper Studio** | 桌面     | 免费   | ⭐⭐⭐⭐   | 轻量级、跨平台      |
| **DataGrip**         | 桌面     | 付费   | ⭐⭐⭐⭐⭐ | 专业开发、团队协作  |
| **RedisInsight**     | 桌面     | 免费   | ⭐⭐⭐⭐⭐ | Redis 管理          |

### 推荐组合

**开发环境**:

- PostgreSQL: **Drizzle Studio** (快速查看) + **DBeaver** (深度管理)
- Redis: **RedisInsight** (官方工具)

**生产环境**:

- 使用命令行工具 + 监控系统 (如 Grafana + Prometheus)

---

## 十二、常见问题

### 1. PostgreSQL 版本 < 18

如果使用 PostgreSQL 18 以下版本，需要修改 UUID 生成方式：

**安装依赖**:

```bash
pnpm add uuid
pnpm add -D @types/uuid
```

**修改文件** `src/db/schema/_shard/base-columns.ts`:

```typescript
// 文件顶部添加
import { uuidV7 } from "uuid";

// 修改 id 字段定义
// 将
uuid().primaryKey().notNull().default(sql`uuidv7()`);
// 改为
uuid().primaryKey().notNull().$defaultFn(() => uuidV7());
```

### 2. 端口被占用

**检查端口占用**:

```bash
# macOS/Linux
lsof -i :9999

# Windows
netstat -ano | findstr :9999
```

**解决方案**:

方式 1: 修改 `.env` 文件中的 `PORT` 配置

```bash
PORT=3000  # 改为其他可用端口
```

方式 2: 杀死占用端口的进程

```bash
# macOS/Linux (替换 PID 为 lsof 命令显示的进程 ID)
kill -9 <PID>

# Windows (替换 PID 为 netstat 命令显示的进程 ID)
taskkill /PID <PID> /F
```

### 3. 数据库连接失败

检查：

- PostgreSQL 服务是否启动
- 数据库连接字符串是否正确
- 防火墙是否阻止连接

```bash
# 测试数据库连接
psql postgresql://postgres:postgres@localhost:5432/postgres
```

### 4. Redis 连接失败

```bash
# 检查 Redis 服务状态
redis-cli ping
# 应返回 PONG

# 查看 Redis 日志
docker-compose logs redis
```

### 5. pnpm 安装依赖失败

```bash
# 清理缓存
pnpm store prune

# 删除 node_modules 和 lockfile 重新安装
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

---

## 十二、项目结构说明

```
clhoria-template/
├── src/
│   ├── routes/          # 路由定义 (按业务模块分层)
│   │   ├── admin/       # 管理端 API
│   │   ├── client/      # 客户端 API
│   │   └── public/      # 公开 API
│   ├── db/              # 数据库相关
│   │   └── schema/      # Drizzle ORM 表定义
│   ├── lib/             # 核心库和工具
│   ├── middlewares/     # 中间件 (认证、日志等)
│   ├── services/        # 业务服务层
│   └── utils/           # 工具函数
├── migrations/          # 数据库迁移文件
├── tests/               # 测试文件
├── .env                 # 环境变量 (需手动创建)
├── docker-compose.yml   # Docker 编排配置
└── package.json         # 项目依赖和脚本
```

---

## 十三、开发流程总结

```bash
# 1. 安装数据库 (Docker 方式)
docker-compose up -d postgres redis

# 2. 安装依赖
pnpm install

# 3. 配置环境变量
cp .env.example .env
# 编辑 .env 文件，修改 JWT 密钥等配置

# 4. 初始化数据库
pnpm push
pnpm seed

# 5. 启动开发服务器
pnpm dev

# 6. 访问 API 文档
# http://localhost:9999
```

---

## 十四、技术栈

- **框架**: Hono (高性能 Web 框架)
- **语言**: TypeScript
- **数据库**: PostgreSQL + Drizzle ORM
- **缓存**: Redis + ioredis
- **认证**: JWT (双密钥隔离)
- **权限**: Casbin (RBAC)
- **文档**: OpenAPI 3.1 + Scalar UI
- **验证**: Zod (类型安全的 schema 验证)
- **任务队列**: pg-boss
- **定时任务**: croner
- **日志**: pino + hono-pino
- **测试**: Vitest
- **构建**: tsdown
- **对象存储**: AWS S3 SDK (兼容 Cloudflare R2、阿里云 OSS)

---

## 十五、相关资源

- **项目仓库**: https://github.com/zhe-qi/clhoria-template
- **配套前端**: https://github.com/zhe-qi/refine-project
- **Hono 文档**: https://hono.dev
- **Drizzle ORM**: https://orm.drizzle.team
- **Casbin**: https://casbin.org

---

## 十六、获取帮助

如有问题或建议，请：

- 创建 [Issue](https://github.com/zhe-qi/clhoria-template/issues)
- 查看项目 [README.md](./README.md)
- 联系项目维护者
